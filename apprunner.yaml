version: 1.0
runtime: python3
build:
  commands:
    build:        
      - pip3 install -r requirements.txt
    post-build:
      - python3 manage.py test
run:
  runtime-version: 3
  command: cd mysite; python3 manage.py runserver
    port: 8000